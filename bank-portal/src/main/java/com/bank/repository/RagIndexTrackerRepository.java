package com.bank.repository;

import com.bank.entity.RagIndexTracker;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RagIndexTrackerRepository extends JpaRepository<RagIndexTracker, Integer> {
}
